apiVersion: v1
kind: Service
metadata:
  name: sftp-server-service
  namespace: cqdg-qa 
spec:
  type: NodePort
  selector:
    app: sftp-server
  ports:
    - protocol: TCP
      port: 22
      targetPort: 22