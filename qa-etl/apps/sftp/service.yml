---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: sftp-server
  name: sftp-server
spec:
  type: NodePort
  ports:
    - port: 8888
      targetPort: 8888
      protocol: TCP
  selector:
    app: sftp-server