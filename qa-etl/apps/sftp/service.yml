---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: sftp-server
  name: sftp-server
spec:
  type: NodePort
  ports:
    - port: 22
      targetPort: 22
      protocol: TCP
  selector:
    app: sftp-server