apiVersion: apps/v1
kind: Deployment
metadata:
  name: sftp-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sftp-server
  template:
    metadata:
      labels:
        app: sftp-server
    spec:
      containers:
      - name: sftp
        image: atmoz/sftp:latest
        ports:
        - containerPort: 8888
        volumeMounts:
        - mountPath: /home/user/upload
          name: sftp-data
        env:
        - name: SFTP_USERS
          value: "user:pass"
        - name: SFTP_UID
          value: "1000"
        - name: SFTP_GID
          value: "1000"
      volumes:
      - name: sftp-data
        persistentVolumeClaim:
          claimName: sftp-pvc
      nodeSelector:
        node: number4