apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: report-api
  name: report-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: report-api
  template:
    metadata:
      labels:
        app: report-api
    spec:
      imagePullSecrets:
      - name: images-pull
      volumes:
      - name: ingress-ca-certificate
        secret:
          secretName: report-api-ca-certificate
          defaultMode: 0555
      containers:
      - image: index.docker.io/ferlabcrsj/api-portal-reports:eac85d4b9e61650e5de27d8176deedd9c6a8c0b6-1700504629 # {"$imagepolicy": "cqdg-qa-fluxcd:report-api"}
        name: report-api
        env:
        - name: NODE_EXTRA_CA_CERTS
          value: /opt/ca-bundle/ca.pem
        envFrom:
        - configMapRef:
            name: report-api-customizations
        volumeMounts:
        - name: ingress-ca-certificate
          mountPath: /opt/ca
          readOnly: true
        - name: ca-bundle
          mountPath: /opt/ca-bundle
          readOnly: true
        resources:
          requests:
            memory: "1024Mi"
            cpu: '0.5'
          limits:
            memory: "2048Mi"
            cpu: '1.00'
