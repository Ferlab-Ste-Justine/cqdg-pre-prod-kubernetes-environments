---
apiVersion: batch/v1
kind: Job
metadata:
  name: restore-blue-2022-09-14-13h50-fhir
spec:
  backoffLimit: 1
  activeDeadlineSeconds: 9000
  template:
    spec:
      restartPolicy: Never
      volumes:
        - name: db-ca-certificate
          secret:
            secretName: postgres-qa-ca-certificate
            defaultMode: 0555
      containers:
        - image: ferlabcrsj/postgres-backup:9bb43092f76e95f17cd09f03a27c65d84112a3cd
          name: restore-blue-2022-09-14-13h50-fhir
          env:
            - name: PGDATABASE
              value: fhir_qa
            - name: PGHOST
              value: postgres-server-blue-qa
            - name: PGSSLMODE
              value: verify-full
            - name: PGSSLROOTCERT
              value: /opt/ca/ca.pem
            - name: NODE_EXTRA_CA_CERTS
              value: /opt/ca/ca.pem
            - name: PGUSER
              valueFrom:
                secretKeyRef:
                  name: postgres-qa-fhir-credentials
                  key: username
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-qa-fhir-credentials
                  key: password
          envFrom:
            - secretRef:
                name: postgres-qa-fhir-credentials
            - secretRef:
                name: s3-fhir-credentials
            - configMapRef:
                name: fhir-s3-restore
          command:
            - python3
            - /opt/restore.py
          volumeMounts:
            - name: db-ca-certificate
              mountPath: /opt/ca
              readOnly: true