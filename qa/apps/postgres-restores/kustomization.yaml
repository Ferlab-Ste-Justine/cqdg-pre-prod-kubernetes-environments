apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: cqdg-qa

configMapGenerator:
  - name: clinical-data-service-s3-restore
    options:
      disableNameSuffixHash: false
    literals:
      - S3_ENDPOINT=https://s3.ops.cqdg.ferlab.bio
      - S3_BUCKET=cqdg-qa-app-clinical-data-service-backups
      - S3_REGION=RegionOne
  - name: datagrid-service-s3-restore
    options:
      disableNameSuffixHash: false
    literals:
      - S3_ENDPOINT=https://s3.ops.cqdg.ferlab.bio
      - S3_BUCKET=cqdg-qa-app-datagrid-service-backups
      - S3_REGION=RegionOne
  - name: hive-metastore-s3-restore
    options:
      disableNameSuffixHash: false
    literals:
      - S3_ENDPOINT=https://s3.ops.cqdg.ferlab.bio
      - S3_BUCKET=cqdg-qa-app-hive-metastore-backups
      - S3_REGION=RegionOne
  - name: id-service-s3-restore
    options:
      disableNameSuffixHash: false
    literals:
      - S3_ENDPOINT=https://s3.ops.cqdg.ferlab.bio
      - S3_BUCKET=cqdg-qa-app-id-service-backups
      - S3_REGION=RegionOne
  - name: keycloak-s3-restore
    options:
      disableNameSuffixHash: false
    literals:
      - S3_ENDPOINT=https://s3.ops.cqdg.ferlab.bio
      - S3_BUCKET=cqdg-qa-app-keycloak-backups
      - S3_REGION=RegionOne
  - name: fhir-s3-restore
    options:
      disableNameSuffixHash: false
    literals:
      - S3_ENDPOINT=https://s3.ops.cqdg.ferlab.bio
      - S3_BUCKET=cqdg-qa-app-fhir-backups
      - S3_REGION=RegionOne
resources:
  - restore_blue_2022_09_14_13h50