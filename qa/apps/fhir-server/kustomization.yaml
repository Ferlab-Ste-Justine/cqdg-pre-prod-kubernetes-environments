---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: cqdg-qa

resources:
  - ingresses.yml
  - services.yml
  - configmaps
  - entrypoint
  - deployment.yml

patchesStrategicMerge:
  - dns-overrides.yml

commonLabels:
  group: cqdg-fhir-server