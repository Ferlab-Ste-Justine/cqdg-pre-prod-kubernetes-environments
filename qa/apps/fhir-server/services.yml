---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: fhir-server
  name: fhir-server
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: fhir-server-green
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: fhir-server-green
  name: fhir-server-green
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: fhir-server-green
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: fhir-server-blue
  name: fhir-server-blue
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: fhir-server-blue