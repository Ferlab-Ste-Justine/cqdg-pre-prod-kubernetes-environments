apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: keycloak-ingress
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  tls:
    - hosts:
      - auth.qa.cqdg.ferlab.bio
      secretName: letsencrypt-certificate-temp
  rules:
    - host: auth.qa.cqdg.ferlab.bio
      http:
        paths:
        - path: /
          backend:
            serviceName: keycloak-http
            servicePort: 80