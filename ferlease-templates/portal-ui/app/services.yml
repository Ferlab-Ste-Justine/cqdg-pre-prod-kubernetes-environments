---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{.Service}}-release-{{.Release}}
  name: {{.Service}}-release-{{.Release}}
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: {{.Service}}-release-{{.Release}}