apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: {{.Service}}-release-{{.Release}}
  name: {{.Service}}-release-{{.Release}}
spec:
  replicas: 2
  selector:
    matchLabels:
      app: {{.Service}}-release-{{.Release}}
  template:
    metadata:
      labels:
        app: {{.Service}}-release-{{.Release}}
    spec:
      imagePullSecrets:
      - name: images-registry-credentials
      containers:
      - image: index.docker.io/ferlabcrsj/cqdg-{{.Service}}:qa-f0772b72b4252eaa87692b752a77b21986e66a40-1680277718 # {"$imagepolicy": "cqdg-{{.Environment}}-fluxcd:{{.Service}}-release-{{.Release}}"}
        name: {{.Service}}-release-{{.Release}}